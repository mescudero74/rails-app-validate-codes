<div class="mx-auto" style="width: 200px;">
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <div class="col-md-12 mb-12">
      <%= f.label :usuario, class: "lead"%>
      <%= f.text_field :username, class: "form-control", id: "validationDefault01"%>
    </div>
  </div>

  <div class="field">
    <div class="col-md-12 mb-12">
      <%= f.label :contraseña, class: "lead"%>
      <%= f.password_field :password, class: "form-control", id: "validationDefault01"%>
    </div>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <div class="col-md-12 mb-12">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    </div>
  <% end %>

  <div class="actions col-md-12 mb-12">
    <%= f.submit "Log in", class: "btn btn-primary" %>
  </div>
<% end %>

</div>
