<div class="mx-auto" style="width: 200px;">
<%= form_with(model: code, local: true) do |form| %>
  <% if code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(code.errors.count, "error") %> prohibited this code from being saved:</h2>

      <ul>
        <% code.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="field">
      <div class="col-md-12 mb-12 lead">
        <%= form.label :email%>
        <%= form.text_field :email, class: "form-control", id: "validationDefault01"%>
      </div>
      <div class="col-md-12 mb-12 lead">
    <%= form.label :precio %>
    <%= form.number_field :amount, class: "form-control", id: "validationDefault02"%>
    </div>
    <br>
    <div class="actions col-md-12 mb-12">
      <%= form.submit value:"Crear", class: "btn btn-primary" %>
<%= link_to codes_path class: "text-decoration-none text-reset" do%>
  <button type="button" class="btn btn-primary">Volver</button>
<%end%>
    </div>

</div>
<% end %>
</div>